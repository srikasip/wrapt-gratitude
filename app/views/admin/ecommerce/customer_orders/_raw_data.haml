%h3 Order
%pre= order.ai(plain: true)

%h3 Charge
%pre= order.charge.ai(plain: true)

%h3 Taxes
%pre= order.tax_transactions.ai(plain: true)

%h3 Shipping Labels
- order.purchase_orders.each do |po|
  %h4= po.order_number
  %pre= po.shipment.ai(plain: true)
  %pre= po.shipping_label.ai(plain: true)
