.row
  .col-sm-12
    %h1 Edit Section

.row
  .col-sm-8
    = render 'form', url: admin_survey_section_path(@survey, @survey_section)

    .links__view-back
      = link_to 'Show', [:admin, @survey_section]
      \|
      = link_to 'Back', admin_survey_sections_path