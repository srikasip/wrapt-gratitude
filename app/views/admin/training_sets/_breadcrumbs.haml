- if params[:action] != 'index'
  .row.breadcrumbs
    .col-sm-12
      = link_to "Training Sets".html_safe, admin_training_sets_path
      - if params[:action] == 'edit'
        &raquo;
        = link_to "#{@training_set.name}".html_safe, [:admin, @training_set]