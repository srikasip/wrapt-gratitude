.row.admin__intro
  .col-sm-12
    %h1 Response Sets

    %p 
      A Response Set is a collection of example survey responses.
      %br
      They are used to evaluate the recommendations generated by the Training Sets.
    %p
      We recommend having at least 5 responses in a set you plan to test. 
      %br
      For example, you might create a set of 'adventurer' responses and another set of 'introvert' responses. 

.row.admin__actions
  .col-sm-12
    = link_to 'New Response Set', new_admin_profile_set_path, class: 'btn btn-primary'
    = link_to 'Export Public Quiz Responses', admin_public_survey_exports_path, class: 'btn btn-primary', method: :create

.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Name
          %th Quiz
          %th

      %tbody
        - @profile_sets.preload(:survey).each do |profile_set|
          %tr
            %td= profile_set.name
            %td= profile_set.survey.title
            %td
              = link_to 'View', admin_profile_set_path(profile_set), class: 'btn btn-xs btn-default'
              = link_to 'Export', admin_profile_set_exports_path(profile_set), method: :post, class: 'btn btn-xs btn-default'
              = link_to 'Delete', [:admin, profile_set], :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-xs btn-danger'


