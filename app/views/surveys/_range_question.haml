= question.prompt
= link_to "Edit", edit_survey_range_question_path(@survey, question), class: 'btn btn-default btn-xs'
= link_to 'Delete', survey_range_question_path(@survey, question), method: :delete, data: {confirm: "This will delete '#{question.prompt}'"}, class: 'btn btn-danger btn-xs'