- title_action = capture_haml do
  .wrapt-dropdown
    -#= link_to '#my-account-giftees__filters', role: 'button', data: {toggle: 'collapse'}, class: 'ma-filter-toggle__link', 'aria-expanded' => false, 'aria-controls' => 'my-account-giftees__filters' do
    -#  TODO: Filters
    -#  = embedded_svg('icon-arrow-down')
    .blue-dropdown__content.right.ma-filters#my-account-giftees__filters.collapse
      :ruby
        options = [
          ['TODO: Filters', 'all']
        ]
      - options.each do |option|
        = link_to my_account_giftees_path() do
          = option[0]
- page_header_action = capture_haml do
  -#
    -# this is broken. I don't know that it ever worked
    -# quick and dirty fix below until there is time to dig into js to make this work
    = link_to 'New Giftee', new_my_account_giftee_path, class: 'btn btn-sm btn-primary uppercase hidden-xs', data: {loads_in_pjax_modal: true}
  = link_to 'New Giftee', '#new-giftee', class: 'btn btn-sm btn-primary uppercase hidden-xs'
.row
  .col-xs-12
    = render 'my_account/page_header', title: 'My Giftees', title_action: title_action, page_header_action: page_header_action
