%h1 Profile Sets

%p A Profile Set is a collection of example survey responses.  They are used to evaluate the recommendations generated by the Training Sets

%p= link_to 'New Profile Set', new_profile_set_path, class: 'btn btn-primary'

%table.table
  %thead
    %tr
      %th Name
      %th

  %tbody
    - @profile_sets.each do |profile_set|
      %tr
        %td= profile_set.name
        %td
          = link_to 'View', profile_set_path(profile_set), class: 'btn btn-xs btn-default'
          = link_to 'Delete', profile_set, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-xs btn-danger'


