.row.admin__intro
  .col-sm-12
    %h1 Profile Sets

    %p 
      A Profile Set is a collection of example survey responses.
      %br
      They are used to evaluate the recommendations generated by the Training Sets.
    %p
      We recommend having at least 5 responses in a set you plan to test. 
      %br
      For example, you might create a set of 'adventurer' responses and another set of 'introvert' responses. 

.row.admin__actions
  .col-sm-12
    = link_to 'New Profile Set', new_profile_set_path, class: 'btn btn-primary'

.row
  .col-sm-12
    %table.table
      %thead
        %tr
          %th Name
          %th

      %tbody
        - @profile_sets.each do |profile_set|
          %tr
            %td= profile_set.name
            %td
              = link_to 'View', profile_set_path(profile_set), class: 'btn btn-xs btn-default'
              = link_to 'Delete', profile_set, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-xs btn-danger'


